# Vistash 素材领域

Vistash 素材领域描述本地图片进入自有库之后的身份、组织和生命周期。术语只表达产品语义，不描述 Tauri、SQLite 或文件路径等实现细节。

## Language

**图片素材（Image Asset）**：
复制进入 Vistash 库并由内容哈希唯一标识的一张本地图片，包含图片本体及其可读元数据。
_Avoid_：链接图片、源文件、附件、在两类素材并列时单称“素材”

**提示词素材（Prompt Asset）**：
以一份当前提示词正文为核心、可独立组织和复用的本地文本素材；图片、标题、模型与参数说明都可以缺省。
_Avoid_：图片属性、提示词版本、生成任务

**普通关联（Image–Prompt Link）**：
图片素材与提示词素材之间由使用者自愿建立的可选多对多关系，不声称图片的生成来源或提示词的推导来源。
_Avoid_：生成记录、反推来源、自动关联

**库（Library）**：
由使用者选择、承载素材及其全部权威元数据的本地集合。
_Avoid_：项目、工作区、数据库

**图片文件夹（Image Folder）**：
库内用于组织图片素材的逻辑节点；它独立存在，不等同于磁盘目录，也可以不包含任何图片。
_Avoid_：目录、物理文件夹、提示词文件夹

**提示词文件夹（Prompt Folder）**：
库内用于组织提示词素材的逻辑节点，与图片文件夹使用两套彼此独立的层级。
_Avoid_：图片文件夹、混合文件夹、项目

**根文件夹（Root Folder）**：
图片或提示词素材不属于其对应文件夹树中任何显式节点时所在的默认逻辑位置。
_Avoid_：未分类文件夹、收件箱

**标签（Tag）**：
图片与提示词共用词表的无层级文本标记；它在两类素材上分别附加、分别计数和分别筛选。
_Avoid_：分类、文件夹、关键词字段

**备注（Note）**：
使用者附加到单个图片或提示词素材的多行纯文本说明，不承载富文本或版本语义。
_Avoid_：Markdown 文档、提示词正文、元数据

**收藏（Favorite）**：
使用者对单个图片或提示词素材设置的二值快速标记，不表示星级、质量或分类。
_Avoid_：星级、评分、颜色标签

**回收站（Trash）**：
图片库与提示词库分别呈现的可恢复删除区域；其中的关联对象在另一侧显式标记为已删除。
_Avoid_：Windows 回收站、垃圾箱目录、硬删除区

**删除（Delete）**：
把图片或提示词素材从其可浏览集合移入对应库内回收站的可恢复状态转换。
_Avoid_：移除、永久删除、清空

**还原（Restore）**：
把回收站中的图片或提示词素材恢复到删除前文件夹；原文件夹不存在时恢复到对应根文件夹。
_Avoid_：重新导入、撤销复制

**清空回收站（Purge Trash）**：
经使用者二次确认后永久删除回收站内全部素材的不可恢复操作。
_Avoid_：删除、清理、重置

**图片查询（Image Query）**：
由文件名文本、共享标签与图片文件夹位置组成、用于缩小当前图片素材集合的检索条件。
_Avoid_：智能文件夹、全文搜索、筛选器配置

**提示词查询（Prompt Query）**：
由标题或正文文本、共享标签与提示词文件夹位置组成、用于缩小当前提示词素材集合的检索条件。
_Avoid_：图片查询、混合无类型搜索
