# Vistash 素材领域

Vistash 素材领域描述本地图片进入自有库之后的身份、组织和生命周期。术语只表达产品语义，不描述 Tauri、SQLite 或文件路径等实现细节。

## Language

**素材（Asset）**：
复制进入 Vistash 库并由内容哈希唯一标识的一张本地图片，包含图片本体及其可读元数据。
_Avoid_：链接图片、源文件、附件

**库（Library）**：
由使用者选择、承载素材及其全部权威元数据的本地集合。
_Avoid_：项目、工作区、数据库

**文件夹（Folder）**：
库内用于组织素材的逻辑节点；它独立存在，不等同于磁盘目录，也可以不包含任何素材。
_Avoid_：目录、物理文件夹、分组

**根文件夹（Root Folder）**：
不属于任何显式文件夹的素材所在的默认逻辑位置。
_Avoid_：未分类文件夹、收件箱

**标签（Tag）**：
使用者附加到素材上的无层级文本标记，同一标签可以属于多个素材。
_Avoid_：分类、文件夹、关键词字段

**回收站（Trash）**：
库内保存已删除素材、支持还原并继续参与去重判定的逻辑区域。
_Avoid_：Windows 回收站、垃圾箱目录、硬删除区

**删除（Delete）**：
把素材从可浏览集合移入库内回收站的可恢复状态转换。
_Avoid_：移除、永久删除、清空

**还原（Restore）**：
把回收站中的素材恢复到删除前文件夹；原文件夹不存在时恢复到根文件夹。
_Avoid_：重新导入、撤销复制

**清空回收站（Purge Trash）**：
经使用者二次确认后永久删除回收站内全部素材的不可恢复操作。
_Avoid_：删除、清理、重置

**素材查询（Asset Query）**：
由文件名文本与标签条件组成、用于缩小当前素材集合的检索条件。
_Avoid_：智能文件夹、全文搜索、筛选器配置
