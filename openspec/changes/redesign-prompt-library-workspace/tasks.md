## 1. 现状基线与公开契约

- [x] 1.1 记录当前提示词页面的 Hallmark 审查、真实 Tauri 截图、功能清单和旧全局样式所有权，明确必须保留与必须删除的行为/视觉
- [x] 1.2 从 `prompt-library/index.ts` 公开出口为 Archive Desk 三栏、单行查询栏、上下文操作显隐、空状态和无重复 eyebrow 编写失败回归
- [ ] 1.3 为卡片/列表共享查询选择、栏位偏好隔离和查询级滚动恢复补充失败回归，不穿透内部 store

## 2. 工作台骨架、导航与查询

- [x] 2.1 建立提示词模块级 CSS、Workbench 骨架和内部真实知识簇边界，保持 `PromptLibraryWorkspace` 公开 interface 不变
- [x] 2.2 实现扁平图标导航：全部、收藏、根位置、文件夹树、标签与回收站，并把文件夹增删改收进侧栏上下文
- [x] 2.3 实现中央集合标题、真实结果数和单行查询栏，只保留搜索、排序、密度与卡片/详情图标切换
- [x] 2.4 重做正常、筛选为空、根位置与回收站空状态，删除 `PROMPT LIBRARY`、`NO PROMPTS` 等装饰标签和大块虚线容器
- [x] 2.5 把全部、收藏、提示词根位置、具体文件夹和回收站改为互斥浏览范围，删除中央区重复条件胶囊并以公开回归锁定切换序列
- [x] 2.6 为 `move_prompt_folder` 先写核心失败测试，再实现事务化子树重映射、Tauri/TypeScript IPC 与错误合同
- [x] 2.7 把提示词文件夹统一为图片侧的树内新建、节点右键、层级视觉、拖动移动和键盘移动 Dialog
- [x] 2.8 与图片侧共享拖动期间的“移到顶层”放置区，验证提示词子树以 `destinationParent=null` 移回顶层

## 3. 提示词卡片与详情列表

- [ ] 3.1 为纯文本卡片、单封面 `+N`、标题缺省正文首行、悬停/焦点/选中动作与媒体租约编写失败回归
- [ ] 3.2 实现正文主导的虚拟卡片瀑布流和可调密度；封面只表达关联上下文，复制/收藏不嵌套卡片主按钮
- [ ] 3.3 为详情列表完整列、响应式列隐藏、列标题和跨视图选择/滚动保持编写失败回归
- [ ] 3.4 实现虚拟详情列表的摘要、文件夹、标签、关联数、模型、收藏和更新时间列，并完成长内容截断/可访问文本

## 4. 单选检查器与聚焦编辑

- [ ] 4.1 为摘要动作唯一入口、正文/组织/备注/关联图片连续分区和无重复定位标签编写失败回归
- [ ] 4.2 实现单选检查器摘要与复制、编辑、收藏、关联和回收站图标动作，统一 Tooltip、焦点、禁用和错误状态
- [ ] 4.3 迁移正文阅读/聚焦编辑，保持 Ctrl+S、保存失败草稿、退出滚动恢复与全局脏草稿拦截
- [ ] 4.4 迁移组织、纯文本备注和关联图片/封面分区，保留库内选择、本地导入、删除态与逐项失败语义
- [x] 4.5 以公开工作区回归实现聚焦式手动新建提示词，覆盖当前文件夹归属、收藏范围回根、Ctrl+S、失败草稿保留和创建后聚焦

## 5. 多选、批量组织与回收站

- [ ] 5.1 为多选共同/混合值、仅多选出现底栏、固定失败目标和跨视图选择保持编写失败回归
- [ ] 5.2 实现只读多选检查器和精简底栏：文件夹、标签、单一收藏状态直接可见，关联与回收站进入更多菜单
- [ ] 5.3 重做提示词还原、永久清空和批量结果呈现，保留关联图片且让危险操作继续阻断确认

## 6. 栏位偏好与响应式

- [ ] 6.1 为提示词独立左右栏宽度、折叠、拖动一次写回、键盘等价和库隔离编写失败回归
- [ ] 6.2 实现宽屏 separator、折叠图标和布局恢复，复用图片栏位交互协议但不共享业务状态
- [ ] 6.3 实现 1050px 分类导航与 780px 检查器覆盖层，验证 1440/1274/960/760 深浅主题无水平溢出和焦点恢复

## 7. 正式切换、性能与最终验收

- [ ] 7.1 将所有提示词生产呈现切换到新模块组件，删除 `styles.css` 中零引用旧提示词规则，不保留 legacy 样式或运行时开关
- [ ] 7.2 在 10,000 项 fixture 上验证卡片/列表初始 DOM、滚动帧、峰值 DOM、内存和视图切换达到既定门禁
- [ ] 7.3 串行通过 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`cargo clippy --workspace --all-targets -- -D warnings` 与 `cargo test --workspace`
- [ ] 7.4 使用 `webapp-testing`、`web-design-guidelines` 和最终 `code-review` 修复全部阻断项，并运行 OpenSpec strict validate
- [ ] 7.5 在真实 Tauri 中由用户确认图片与提示词属于同一设计系统，记录最终截图、剩余非阻断差异和验收结论

## 8. Code review 修复

- [x] 8.1 统一“提示词根位置”领域术语，并把工作区行为回归改为只从 `prompt-library/index.ts` 公共 interface 进入
- [x] 8.2 为新建提示词接入全局脏草稿守卫并冻结打开时归属，覆盖切范围、切库与关闭窗口不丢稿
- [x] 8.3 把拖放目标改为判别联合，抽取共享文件夹路径工具与无业务状态的移动目标表单
- [x] 8.4 把提示词导航和文件夹组织从 `PromptWorkspace` 拆为内部深模块，保持公共 interface 与行为不变
- [x] 8.5 复用图片与提示词共享的文件夹树、顶层放置区和树内新建表单，删除叶节点命名 fallback 并保持两侧行为一致
