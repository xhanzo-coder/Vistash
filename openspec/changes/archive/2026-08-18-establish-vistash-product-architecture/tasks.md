本变更的范围已按 `design.md` 第十八条收缩：只完成素材管理框架所需的契约，反推侧的字段级契约整体移交后继变更，见文末"移交后继变更的任务"。

## 1. 供应商与目标模型调研

- [x] 1.1 使用 `research` 核实候选视觉模型供应商的图像输入方式、严格结构化输出支持的 JSON Schema 子集范围、中文精细描述稳定性与计费方式，并把结论写入 `design.md` 待确定问题一
- [x] 1.2 使用 `research` 核实候选国内生图模型的提示词偏好顺序、长度上限、负向约束表达方式、参考图通道与画幅参数，并把结论写入 `design.md` 待确定问题二
- [x] 1.4 依据 1.2 的结论确定 v1 对目标生图模型的处理方式：按使用者决定不绑定具体型号，记录方舟 `doubao-seedream-5-0-pro-260628` 兜底档案与后续正式选型的触发条件，并解除待确定问题二

## 2. 契约定义（框架范围）

- [x] 2.5 定义导入域、回收站域与色卡域的错误码及其触发条件，并明确禁止静默兼容的判定方式（`observe` 与 `compile` 两域随反推侧契约后移）
- [x] 2.6 定义 `target_profile` 结构，包含提示顺序、长度上限、负向约束表达方式、画幅参数与版本标识

## 3. 存储契约定义

- [x] 3.1 定义库格式，包含内容哈希算法、素材本体寻址目录结构与元数据文件格式，并解除待确定问题三
- [x] 3.2 逐字段核对 SQLite 索引可从元数据文件重建，列出无法重建的字段并给出处理方式（本次只核对已定义字段；提示词记录表与反推任务表随 3.5、3.6 后移）
- [x] 3.3 定义素材与库内副本的删除语义，明确库内删除是否进入回收站以及源文件被删后库内副本的地位，并解除待确定问题四
- [x] 3.4 定义导入管线的错误语义与回滚边界，覆盖复制失败、内容重复、空间不足与中途中断四种情形
- [x] 3.7 定义色卡结果契约，包含算法版本标识、代表颜色值、颜色角色、归一占比与显式状态字段

## 4. 规范文件同步

- [x] 4.1 更新 `AGENTS.md` 的项目使命段落，使其同时包含素材管理与反推提示词两项能力，并明确排除网页素材抓取，消除仓库中并存的两个产品定义
- [x] 4.2 更新 `openspec/config.yaml` 的 `context` 段落，使产品目标表述与 `AGENTS.md` 一致
- [x] 4.3 在 `AGENTS.md` 工程门禁中补充媒体处理归属 Rust 侧与禁止内置供应商密钥两条约束
- [x] 4.4 确认本变更新增与修改的全部文本文件均为无 BOM 的 UTF-8

## 5. 验证

- [x] 5.1 运行 `openspec validate establish-vistash-product-architecture --strict --no-interactive` 并修正全部校验问题
- [x] 5.2 按仓库规范与本变更需求审查全部产物，确认四份能力规格的每条需求均至少有一个可测试场景（21 条需求 / 69 个场景全部合格；另修正 4 处交叉引用漂移：决策条数与来源说明、色卡状态字段名、提案未声明范围收缩）
- [x] 5.3 复核 `design.md` 的全部待确定问题状态，确认已解除的条目在正文中同步更新，未解除的条目明确记录其阻塞的后续任务

## 移交后继变更的任务

以下任务原属本变更，按 `design.md` 第十八条移交后继变更，编号保留以便追溯。它们不计入本变更进度，但其对应的能力规格需求已在本变更中写就并会随归档生效，后继变更填写字段时必须满足这些需求。

- 1.3 依据 1.1 的结论选定 v1 视觉模型供应商，在设计中记录选定理由与被排除候选，并解除待确定问题一（阻塞项：需使用者配置 `ARK_API_KEY` 以完成"严格 schema 与图像输入并用"的实证调用）
- 2.1 定义统一观察 schema 骨架，并确认其完整落在 1.3 选定供应商的结构化输出能力范围内（阻塞于 1.3）
- 2.2 定义摄影人像专用观察 schema 的必填维度与互斥规则（阻塞于 2.1）
- 2.3 定义动漫插画专用观察 schema 的必填维度与互斥规则，并包含风格冲突审计规则（阻塞于 2.1）
- 2.4 定义通用观察 schema 的覆盖范围，明确静物与混合类型的落位策略，并解除待确定问题五（阻塞于 2.1）
- 2.7 定义编译阶段提示词骨架，确保只输出自然语言、严格保留必须保留项、且不新增观察结果中无证据的实体与专名（阻塞于 2.1）
- 2.8 定义观察结果校验规则与至多一次定向重分析的触发条件与采纳条件（阻塞于 2.1）
- 2.9 补齐 `observe` 与 `compile` 两域的错误码，覆盖 schema 校验失败、必填字段缺失、语义互斥冲突与模型调用失败（阻塞于 2.1，原属 2.5）
- 3.5 定义提示词记录表字段，并确认 append-only 语义在字段层面成立（形状取决于观察结果的实际结构，阻塞于反推最窄纵向切片实跑）
- 3.6 定义反推任务表字段与状态机转移规则，包含幂等键构成、各阶段耗时与失败原因记录（同上）
- 3.8 核对提示词记录与反推任务两类记录的索引可重建性（阻塞于 3.5、3.6，原属 3.2）
