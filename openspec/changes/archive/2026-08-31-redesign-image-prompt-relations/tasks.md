## 1. 公共关系契约与失败回归

- [x] 1.1 从应用根公共 seam 为关联对象双向打开、正常区/回收站定位和提示词草稿守卫编写失败回归
- [x] 1.2 用匿名内存 Adapter 锁定“提示词侧写入后图片详情旧缓存”并验证按库队列与刷新隔离
- [x] 1.3 定义 `ImagePromptRelations` 判别联合、刷新 Adapter、逐项结果与稳定刷新错误，不暴露内部 revision 或 query key

## 2. 深 Module 与一致性协调

- [x] 2.1 实现生产 Tauri Adapter、内存 Adapter 和按 `LibraryId` 隔离的写入—刷新队列
- [x] 2.2 接入现有建立、解除与封面 IPC，使成功写入精确失效图片详情和提示词快照
- [x] 2.3 实现 `open(target)` 对 `WorkspaceNavigation` 的正常区/回收站定位并复用全局草稿守卫
- [x] 2.4 以公共回归验证写入成功但刷新失败、永久删除竞态和切库不串关系

## 3. 双侧关联交互

- [x] 3.1 重做图片检查器关联提示词对象卡片：标题/摘要/模型/删除态、整行打开与次级解除
- [x] 3.2 重做提示词检查器关联图片对象卡片：缩略图/显示名/文件夹/尺寸/封面/删除态、整格打开与次级菜单
- [x] 3.3 实现两侧搜索多选“添加关联” Dialog，显式标记已关联项、提交关系数量和逐项失败
- [x] 3.4 保留从本地导入并关联、已入库但未关联报告和封面确定性回落

## 4. 验收与交付

- [x] 4.1 在深浅主题与 1440/960/760 宽度验证对象卡片、Dialog、键盘、焦点和无水平溢出
- [x] 4.2 串行通过前端/Rust完整门禁、`web-design-guidelines`、双轴 `code-review` 与 strict validate
- [x] 4.3 在真实 Tauri 中验证双向打开、写后即读与回收站定位，并记录不含用户素材的验收结果
