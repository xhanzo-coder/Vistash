## 1. 视觉原型与技术事实冻结

- [x] 1.1 基于官方文档调研并最小验证 Windows `CF_HDROP`、位图剪贴板、默认程序打开和临时副本文件占用行为，把选型与限制补入 `design.md`
- [x] 1.2 使用固定假数据制作独立高保真深色原型，覆盖欢迎页、空库、瀑布流、详情列表、单选检查器、多选操作栏、灯箱、搜索、任务中心和设置
- [x] 1.3 为原型补齐浅色主题与窄窗口状态，并验证键盘焦点、对比度、减少动态效果和无水平滚动
- [x] 1.4 完成应用图标、紧凑字标、强调色、字体与唯一图标系统的轻量品牌评审，并把最终 token、断点、动效与品牌资产决策补入 `design.md`
- [x] 1.5 记录原型截图与评审结论，明确批准或拒绝可复用代码，冻结生产实施所需的视觉验收基线

## 2. 新库格式与素材领域模型

- [x] 2.1 先为显式 `AssetSource`、必填显示文件名和单一可选文件夹归属编写失败的 Rust 序列化与不变量测试
- [x] 2.2 实现新素材侧车类型、显示文件名校验和真实扩展名语义，使新领域测试通过
- [x] 2.3 先通过 v3 侧车公开 interface 为显示文件名修改、非法修改不覆盖旧值和来源字段不可变编写失败测试
- [x] 2.4 实现 v3 侧车显示文件名修改，使来源身份、内容哈希对象和真实扩展名保持不变
- [x] 2.5 先通过 v3 侧车公开 interface 为移动到唯一文件夹、移动到未分类和非法目标不覆盖旧值编写失败测试
- [x] 2.6 实现 v3 侧车单归属移动操作，但在迁移提交完成前不切换现有 Catalog、索引或批量命令

## 3. 旧库迁移与恢复

- [x] 3.1 先用零归属、单归属和多归属 fixture 为只读迁移计划与冲突列表编写失败测试
- [x] 3.2 实现迁移计划、侧车摘要校验和使用者唯一目标选择模型，不在计划阶段写库
- [x] 3.3 先为同卷暂存、恢复日志、任一步写入失败和进程中断后的恢复编写失败测试
- [x] 3.4 实现新格式暂存、旧元数据备份、原子替换、索引重建、完整校验与整体回滚
- [x] 3.5 更新库格式版本、打开门禁和 Tauri 迁移 command，并验证旧库冲突确认前不能进入工作区
- [x] 3.6 先为迁移后的 v3 侧车索引重建、显示名/来源名双查询和 Catalog 单归属写入编写失败测试
- [x] 3.7 在迁移提交门禁后把生产侧车别名、SQLite 索引、Catalog 与批量命令切换到 v3，并删除图片侧多归属写入 interface

## 4. 统一导入、停止与目录层级

- [x] 4.1 先为文件导入目标、文件夹相对层级、当前文件夹父级和同逻辑路径合并编写失败测试
- [x] 4.2 实现统一 `ImportSource` 协调器与目录层级映射，保证重复素材不被移动且非图片计入跳过结果
- [x] 4.3 先为任务 ID、库级导入并发键、扫描取消和单素材事务边界停止编写失败测试
- [x] 4.4 实现后端停止命令与 `running/stopping/stopped` 状态，使已成功项保留、当前项成功或回滚、后续项计入未处理
- [x] 4.5 更新 Tauri 导入 channel、进度和完成报告 DTO，并验证按钮、拖放与目录选择复用同一协调器

## 5. Windows 剪贴板与出站能力

- [x] 5.1 按任务 1.1 的选型实现 `ClipboardPort` 生产 adapter 与内存 adapter，并用 contract tests 验证文件列表优先、位图与纯文本分流
- [x] 5.2 先为资源管理器文件/目录粘贴、Rust 位图 PNG 编码和文本编辑控件不认领 `Ctrl+V` 编写失败测试
- [x] 5.3 实现剪贴板导入 command、窗口快捷键认领与权限配置，确保前端不接收像素缓冲
- [x] 5.4 先为导出原图、显示文件名、跳过/覆盖/自动编号冲突计划、逐项失败和安全停止编写失败测试
- [x] 5.5 实现导出协调器、冲突确认和 Tauri command，保证库内本体与侧车不被修改
- [x] 5.6 实现单图复制位图与默认程序只读临时副本，验证多选不合成、外部修改不影响权威对象且缓存清理只作用于记录目录

## 6. 应用服务与平台 seam

- [x] 6.1 定义并测试类型化 `WorkspaceNavigation`、`TaskCenter`、`GlobalSearch` 与 platform interfaces，禁止任意字符串事件和业务 fallback
- [x] 6.2 实现 Tauri/Memory platform adapters，并用共享 contract tests 验证错误码、媒体租约、文件对话框、拖放和取消监听
- [ ] 6.3 实现任务中心 store、并发键、节流进度、真实停止确认和完成报告保留规则
- [ ] 6.4 建立 `library-lifecycle`、`asset-library`、`prompt-library` 唯一公共出口，并添加禁止跨模块导入 `internal/` 的 lint 或结构检查

## 7. UI 基础、主题与应用外壳

- [ ] 7.1 安装经原型批准的 Radix Primitives、TanStack Query 和唯一图标依赖，配置 QueryClient 的无重试、无焦点全刷与有限缓存策略
- [ ] 7.2 实现 reset、批准的品牌字体与语义 design token、CSS Modules 基础、system/dark/light 主题切换和 Tauri 全尺寸图标资产，并用测试验证减少动态效果
- [ ] 7.3 实现经原型批准的 Button、Dialog、Menu、Popover、Tooltip、SearchField、Progress、Toast 等无业务 UI 模块
- [ ] 7.4 实现原生标题栏下的紧凑顶栏、一级入口、全局搜索、导入、任务中心和设置 Dialog
- [ ] 7.5 实现欢迎、开库失败、迁移冲突和兼容库会话的 `library-lifecycle` 界面，并验证无首页直接恢复工作现场

## 8. 图片集合、查询与选择

- [ ] 8.1 先从 `asset-library` 公开 interface 为查询恢复、全局定位、视图切换和 `libraryId` 隔离编写失败行为测试
- [ ] 8.2 实现图片模块 session、内部 query key、精确失效和布局偏好，删除 `catalogVersion`/`refreshVersion` 外壳协议
- [ ] 8.3 实现左栏“全部图片、收藏、未分类、文件夹树、回收站”和标签筛选面板，并验证本地 `Ctrl+F` 与全局 `Ctrl+K` 分层
- [ ] 8.4 实现可调密度的虚拟瀑布流与虚拟详情列表，共享查询、排序、选择、活动项和滚动恢复
- [ ] 8.5 实现单击、`Ctrl+单击`、`Shift+单击`、`Ctrl+A`、框选、`Esc`、底部多选操作栏与右键快捷菜单

## 9. 组织、检查器与文件名

- [ ] 9.1 实现拖到文件夹即移动、批量移动、文件夹创建/重命名/删除及未分类回落，并验证事务失败语义
- [ ] 9.2 实现单张 `F2`/检查器显示文件名编辑、来源文件名展示与双文件名搜索错误保留
- [ ] 9.3 实现纵向可折叠单选检查器的摘要、色卡、组织、备注、关联提示词和文件信息分区
- [ ] 9.4 实现多选检查器的共同值、混合值以及移动、标签、收藏、提示词关联与回收站批量操作
- [ ] 9.5 更新删除、还原和清空回收站界面，使唯一原文件夹恢复、缺失路径回落未分类和逐项失败可见

## 10. 灯箱、传输界面与反馈

- [ ] 10.1 实现窗口内沉浸式灯箱、当前查询相邻导航、适合窗口、100% 比例、缩放、平移、背景切换和 `Esc` 上下文恢复
- [ ] 10.2 实现图片/文件夹选择、整窗口拖放、工作区粘贴和空库导入引导，并接入统一任务中心
- [ ] 10.3 实现导入/导出任务详情、停止按钮、正在停止状态、冲突确认、完成报告和逐项错误码
- [ ] 10.4 实现导出原图、复制图像、默认程序打开和短暂成功通知，并验证重要失败不只使用会消失的 Toast
- [ ] 10.5 验证 Blob URL/媒体租约在虚拟项卸载、换源、灯箱关闭、缓存淘汰和切库时全部释放

## 11. 切换、回归与最终门禁

- [ ] 11.1 保持提示词工作区现有行为与一级入口，验证普通图片—提示词关联和全局定位不依赖图片模块内部实现
- [ ] 11.2 用新模块 interface 测试替换被深模块吸收的浅层测试，并保留纯算法内部 seam 的必要确定性测试
- [ ] 11.3 切换 `App.tsx` 到新模块，删除旧图片工作区、旧全局样式、旧刷新协议和未批准原型代码，不保留 legacy 开关
- [ ] 11.4 在 10,000 项 fixture 上测量首屏、DOM、滚动帧时间、内存和视图切换，并达到任务 1.5 冻结的基线
- [ ] 11.5 使用 release Tauri 构建验收 Windows 文件/目录拖放、文件剪贴板、截图位图、文本粘贴、对话框、默认程序打开、原生标题栏和窄窗口
- [ ] 11.6 串行通过 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`cargo clippy --workspace --all-targets -- -D warnings` 与 `cargo test --workspace`
- [ ] 11.7 使用 `webapp-testing` 与 `web-design-guidelines` 完成键盘、焦点、语义、深浅主题和视觉状态审查，并修复全部阻断问题
- [ ] 11.8 运行最终 `code-review` 与 `openspec validate redesign-image-library-workspace --strict --no-interactive`，同步任务、设计、规格和实现状态
