## 1. 视觉原型与技术事实冻结

- [x] 1.1 基于官方文档调研并最小验证 Windows `CF_HDROP`、位图剪贴板、默认程序打开和临时副本文件占用行为，把选型与限制补入 `design.md`
- [x] 1.2 使用固定假数据制作独立高保真深色原型，覆盖欢迎页、空库、瀑布流、详情列表、单选检查器、多选操作栏、灯箱、搜索、任务中心和设置
- [x] 1.3 为原型补齐浅色主题与窄窗口状态，并验证键盘焦点、对比度、减少动态效果和无水平滚动
- [x] 1.4 完成应用图标、紧凑字标、强调色、字体与唯一图标系统的轻量品牌评审，并把最终 token、断点、动效与品牌资产决策补入 `design.md`
- [x] 1.5 记录原型截图与评审结论，明确批准或拒绝可复用代码，冻结生产实施所需的视觉验收基线

## 2. 新库格式与素材领域模型

- [x] 2.1 先为显式 `AssetSource`、必填显示文件名和单一可选文件夹归属编写失败的 Rust 序列化与不变量测试
- [x] 2.2 实现新素材侧车类型、显示文件名校验和真实扩展名语义，使新领域测试通过
- [x] 2.3 先通过 v3 侧车公开 interface 为显示文件名修改、非法修改不覆盖旧值和来源字段不可变编写失败测试
- [x] 2.4 实现 v3 侧车显示文件名修改，使来源身份、内容哈希对象和真实扩展名保持不变
- [x] 2.5 先通过 v3 侧车公开 interface 为移动到唯一文件夹、移动到未分类和非法目标不覆盖旧值编写失败测试
- [x] 2.6 实现 v3 侧车单归属移动操作，但在迁移提交完成前不切换现有 Catalog、索引或批量命令

## 3. 旧库迁移与恢复

- [x] 3.1 先用零归属、单归属和多归属 fixture 为只读迁移计划与冲突列表编写失败测试
- [x] 3.2 实现迁移计划、侧车摘要校验和使用者唯一目标选择模型，不在计划阶段写库
- [x] 3.3 先为同卷暂存、恢复日志、任一步写入失败和进程中断后的恢复编写失败测试
- [x] 3.4 实现新格式暂存、旧元数据备份、原子替换、索引重建、完整校验与整体回滚
- [x] 3.5 更新库格式版本、打开门禁和 Tauri 迁移 command，并验证旧库冲突确认前不能进入工作区
- [x] 3.6 先为迁移后的 v3 侧车索引重建、显示名/来源名双查询和 Catalog 单归属写入编写失败测试
- [x] 3.7 在迁移提交门禁后把生产侧车别名、SQLite 索引、Catalog 与批量命令切换到 v3，并删除图片侧多归属写入 interface

## 4. 统一导入、停止与目录层级

- [x] 4.1 先为文件导入目标、文件夹相对层级、当前文件夹父级和同逻辑路径合并编写失败测试
- [x] 4.2 实现统一 `ImportSource` 协调器与目录层级映射，保证重复素材不被移动且非图片计入跳过结果
- [x] 4.3 先为任务 ID、库级导入并发键、扫描取消和单素材事务边界停止编写失败测试
- [x] 4.4 实现后端停止命令与 `running/stopping/stopped` 状态，使已成功项保留、当前项成功或回滚、后续项计入未处理
- [x] 4.5 更新 Tauri 导入 channel、进度和完成报告 DTO，并验证按钮、拖放与目录选择复用同一协调器

## 5. Windows 剪贴板与出站能力

- [x] 5.1 按任务 1.1 的选型实现 `ClipboardPort` 生产 adapter 与内存 adapter，并用 contract tests 验证文件列表优先、位图与纯文本分流
- [x] 5.2 先为资源管理器文件/目录粘贴、Rust 位图 PNG 编码和文本编辑控件不认领 `Ctrl+V` 编写失败测试
- [x] 5.3 实现剪贴板导入 command、窗口快捷键认领与权限配置，确保前端不接收像素缓冲
- [x] 5.4 先为导出原图、显示文件名、跳过/覆盖/自动编号冲突计划、逐项失败和安全停止编写失败测试
- [x] 5.5 实现导出协调器、冲突确认和 Tauri command，保证库内本体与侧车不被修改
- [x] 5.6 实现单图复制位图与默认程序只读临时副本，验证多选不合成、外部修改不影响权威对象且缓存清理只作用于记录目录

## 6. 应用服务与平台 seam

- [x] 6.1 定义并测试类型化 `WorkspaceNavigation`、`TaskCenter`、`GlobalSearch` 与 platform interfaces，禁止任意字符串事件和业务 fallback
- [x] 6.2 实现 Tauri/Memory platform adapters，并用共享 contract tests 验证错误码、媒体租约、文件对话框、拖放和取消监听
- [x] 6.3 实现任务中心 store、并发键、节流进度、真实停止确认和完成报告保留规则
- [x] 6.4 建立 `library-lifecycle`、`asset-library`、`prompt-library` 唯一公共出口，并添加禁止跨模块导入 `internal/` 的 lint 或结构检查
- [x] 6.5 为 v2→v3 开库门禁和 Tauri managed-state 精确类型编写失败回归测试
- [x] 6.6 修复 v2 库迁移信号与 `import_stop` 的 `Arc<TransferRuns>` State 接线，保证旧库不进入 v3 Catalog
- [x] 6.7 为显示文件名修改和导出冲突只读规划的 Catalog/Tauri/TypeScript IPC 编写失败合同测试
- [x] 6.8 实现显示文件名修改与 `plan_export` IPC，注册命令并补齐前端 DTO/封装
- [x] 6.9 为任务 ID 跨进度、结果和停止 IPC 的一致性编写失败测试，覆盖旧任务不能误停新任务
- [x] 6.10 把任务 ID 接入导入/导出 DTO 与停止命令，并把共享运行状态从 `ImportRuns` 深化为传输任务语义
- [x] 6.11 为 external-open manifest 损坏、只读属性失败和清理清单回写失败编写失败测试
- [x] 6.12 修复 external-open 静默 fallback，只允许 manifest 不存在表示空，其余失败显式返回稳定错误
- [x] 6.13 为模块公开 interface 引入 `LibraryId` 与 `AssetId` 品牌类型，并用类型检查与模块边界测试防止身份混传

## 7. UI 基础、主题与应用外壳

- [x] 7.1 安装经原型批准的 Radix Primitives、TanStack Query 和唯一图标依赖，配置 QueryClient 的无重试、无焦点全刷与有限缓存策略
- [x] 7.2 实现 reset、批准的品牌字体与语义 design token、CSS Modules 基础、system/dark/light 主题切换和 Tauri 全尺寸图标资产，并用测试验证减少动态效果
- [x] 7.3 实现经原型批准的 Button、Dialog、Menu、Popover、Tooltip、SearchField、Progress、Toast 等无业务 UI 模块
- [x] 7.4 实现原生标题栏下的紧凑顶栏、一级入口、全局搜索、导入和设置 Dialog
- [x] 7.5 实现欢迎、开库失败、迁移冲突和兼容库会话的 `library-lifecycle` 界面，并验证无首页直接恢复工作现场
- [x] 7.6 用真实长冲突列表浏览器回归修复迁移页及组件展台的高度约束，保证底部操作可达
- [x] 7.7 修复搜索清除后的焦点与布局稳定性，覆盖鼠标、键盘、Escape 和禁用状态
- [x] 7.8 用原生单选控件补齐主题选择的方向键与 Tab 语义，并保持既定外观
- [x] 7.9 将紧凑文字按钮恢复为至少 12px，并验证高度与相邻控件布局
- [x] 7.10 串行完成上述修复的前端/Rust 门禁、深浅主题浏览器复验与审查记录

## 8. 图片集合、查询与选择

- [x] 8.1 先从 `asset-library` 公开 interface 为查询恢复、全局定位、视图切换和 `libraryId` 隔离编写失败行为测试
- [x] 8.2 实现图片模块 session、内部 query key、精确失效和布局偏好；新 interface 不再暴露 `catalogVersion`/`refreshVersion`，旧外壳协议随 11.3 一并删除
- [x] 8.3 实现左栏“全部图片、收藏、未分类、文件夹树、回收站”和标签筛选面板，并验证本地 `Ctrl+F` 与全局 `Ctrl+K` 分层
- [x] 8.4 实现可调密度的虚拟瀑布流与虚拟详情列表，共享查询、排序、选择、活动项和滚动恢复
- [x] 8.5 实现单击、`Ctrl+单击`、`Shift+单击`、`Ctrl+A`、框选、`Esc`、底部多选操作栏与右键快捷菜单

## 9. 组织、检查器与文件名

- [x] 9.1 实现拖到文件夹即移动、批量移动、文件夹创建/重命名/删除及未分类回落，并验证事务失败语义
- [x] 9.2 实现单张 `F2`/检查器显示文件名编辑、来源文件名展示与双文件名搜索错误保留
- [x] 9.3 实现纵向可折叠单选检查器的摘要、色卡、组织、备注、关联提示词和文件信息分区
- [x] 9.4 实现多选检查器的共同值、混合值以及移动、标签、收藏、提示词关联与回收站批量操作
- [x] 9.5 更新删除、还原和清空回收站界面，使唯一原文件夹恢复、缺失路径回落未分类和逐项失败可见

## 10. 灯箱、传输界面与反馈

- [x] 10.1 实现窗口内沉浸式灯箱、当前查询相邻导航、适合窗口、100% 比例、缩放、平移、背景切换和 `Esc` 上下文恢复
- [x] 10.2 实现图片/文件夹选择、整窗口拖放、工作区粘贴和空库导入引导，并接入统一内部任务协调
- [x] 10.3 实现导入/导出内部停止状态、冲突确认、就地完成报告和逐项错误码，不提供全局任务浏览入口
- [x] 10.4 实现导出原图、复制图像、默认程序打开和短暂成功通知，并验证重要失败不只使用会消失的 Toast
- [x] 10.5 验证 Blob URL/媒体租约在虚拟项卸载、换源、灯箱关闭、缓存淘汰和切库时全部释放

## 11. 切换、回归与最终门禁

- [x] 11.1 保持提示词工作区现有行为与一级入口，验证普通图片—提示词关联和全局定位不依赖图片模块内部实现
- [x] 11.2 用新模块 interface 测试替换被深模块吸收的浅层测试，并保留纯算法内部 seam 的必要确定性测试
- [x] 11.3 切换 `App.tsx` 到新模块，删除旧图片工作区、旧全局样式、旧刷新协议和未批准原型代码，不保留 legacy 开关
- [x] 11.4 在 10,000 项 fixture 上测量首屏、DOM、滚动帧时间、内存和视图切换，并达到任务 1.5 冻结的基线
- [ ] 11.5 使用 release Tauri 构建验收 Windows 文件/目录拖放、文件剪贴板、截图位图、文本粘贴、对话框、默认程序打开、自定义标题栏拖动/双击/窗口控件、Windows 贴靠和窄窗口
- [x] 11.6 串行通过 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`cargo clippy --workspace --all-targets -- -D warnings` 与 `cargo test --workspace`
- [x] 11.7 使用 `webapp-testing` 与 `web-design-guidelines` 完成键盘、焦点、语义、深浅主题和视觉状态审查，并修复全部阻断问题
- [x] 11.8 运行最终 `code-review` 与 `openspec validate redesign-image-library-workspace --strict --no-interactive`，同步任务、设计、规格和实现状态

## 12. 用户实机反馈后的前端纠偏

2026-08-29：用户截图确认生产界面偏离已批准的 Archive Desk A，撤回“前端视觉已完成”的结论，重新打开 11.7、11.8。此前测试记录保留为功能证据，不代表视觉验收。以下任务完成前不归档。

- [x] 12.1 按 A 原型恢复侧栏与检查器从顶栏下方开始的三栏结构、中央集合标题、扁平图标导航与侧栏文件夹操作；通过公开界面回归和宽窄截图检查
- [x] 12.2 整理紧凑查询工具栏与唯一主导入入口，区分集合筛选和图片上下文操作，消除重复导入及常驻无关按钮；验证快捷键与错误反馈仍可达
- [x] 12.3 恢复原画幅图片优先的卡片、合理默认密度与悬停/键盘/选择信息层级，复验 10,000 项性能与双主题截图
- [x] 12.4 核对并补齐侧栏/检查器宽度与折叠记忆、详情列表信息列、检查器和批量操作的视觉及交互缺口
- [x] 12.5 在真实 App 中与 A 原型同尺寸对照，记录用户逐项反馈及修正结果；经用户确认视觉方向后再完成最终验收
- [x] 12.6 把全部图片、收藏、未分类、具体文件夹和回收站改为互斥浏览范围，保留搜索与标签，并以公开界面回归锁定切换序列
- [x] 12.7 为每个图片文件夹节点实现新建子文件夹、重命名、移动和删除的右键菜单，保留显式及键盘等价入口
- [x] 12.8 新增事务化 `move_folder` 深命令，实现文件夹拖动重组子树、根位置投放、非法后代/碰撞拒绝和逐项进度，验证不与图片拖动载荷混淆
- [x] 12.9 修正全部图片与普通文件夹范围使用 `favorite=null`，移除侧栏标题处重命名/删除入口并复验收藏仍出现在全部图片
- [x] 12.10 移除独立剪贴板按钮，接入系统 paste 事件、明确结果反馈、Win32 位图存在性与读取错误语义，并优化位图编码和导入耗时
- [x] 12.11 新增当前算法色卡重算命令与恢复入口，降低采样成本并把八行色块表重做为比例色带和紧凑图例
- [x] 12.12 让全部图片、收藏、未分类、具体文件夹和回收站切换时清空文本与标签，验证当前文件夹导入可见、全部图片导入归未分类
- [x] 12.13 删除工具栏下及底部的纯成功结果条，把剪贴板空内容、重复和失败迁移为 Toast，仅保留异常报告
- [x] 12.14 强化文件夹父子树缩进/图标/分支，并把瀑布流白色全宽说明栏改成图片内部浮层
- [x] 12.15 导入成功后按目标范围清空隐藏条件并揭示新图片，验证具体文件夹与全部图片的归属和可见性
- [x] 12.16 把新建文件夹改为树内即时输入，父位置由当前节点自动决定，验证 Enter/Esc、错误保留与无 Dialog
- [x] 12.17 从 AppShell 顶栏删除任务中心入口与组合 props，保留内部并发/状态协调，并把卡片胶囊改成无边框渐隐信息层
- [x] 12.18 移除标签名后无分隔计数，保留可访问用量语义，并以公开工作区回归验证新标签不显示为“名称1”
- [x] 12.19 把文件夹孤立横向分支改为竖向层级引导，为嵌套节点增加“移到顶层”快捷入口，验证 `destinationParent=null` 和移动 Dialog 的明确文案
- [x] 12.20 把详情列表收敛为连续数据表面，统一列对齐、缩略图、标签/格式辅助形态与悬停/选中反馈，并完成亮暗主题实机检查
- [x] 12.21 关闭 Tauri 原生标题栏，在紧凑顶栏接入官方拖动区、最小化、最大化/还原和关闭 API，补齐 capability 与配置合同测试
- [x] 12.22 实现顶栏素材库切换器，显示当前库名/路径并提供新建和打开其他库，删除图片导航中的重复库名
- [x] 12.23 调整 `LibraryLifecycleControls` 为原地发起 create/open 选库意图，验证取消目录对话框保留原库、成功后以新 session 重建工作现场
- [x] 12.24 用回收站四画幅 fixture 建立 120 帧几何稳定门禁，禁止瀑布流横向溢出并固定纵向滚动条 gutter
- [x] 12.25 把缩略图长边提高至 1024、格式版本提升至 3，以显式 `ThumbnailSource` 从正常/回收站权威本体重建，并用 Rust 测试锁定超长图派生尺寸与旧缩略图安全清理
- [x] 12.26 在真实 Tauri 回收站复跑 120 帧探针、灯箱清晰度对照与完整门禁，记录派生缩略图重建结果
- [x] 12.27 以延迟 folder query 锁定“全部图片→移动→文件夹”过渡语义，保留左栏占位元数据但禁止中央区渲染上一范围卡片和数量
- [x] 12.28 在图片和提示词文件夹拖动期间显示并命中“移到顶层”放置区，以公开回归验证 `destinationParent=null` 且不误触素材移动
- [x] 12.29 修正文件夹拖动在 pointerdown 过早捕获导致真实单击丢失，以阈值前不捕获回归和真实导入库精确文件夹检查验证
- [x] 12.30 让 `AssetTransfer` 持续呈现导入部分失败/未处理报告及稳定错误码，以公开工作区回归锁定无任务入口时的可见性
- [x] 12.31 让异常导入报告按素材库隔离并跨新任务、切库重挂载持续保留，删除生命周期页残留任务入口并统一规格与验收记录
- [x] 12.32 统一导入与导出异常报告存储，补齐跳过原因、整体失败、逐项稳定身份和跨选择/切库明确关闭回归，并排除真实用户库截图与硬编码探针
